server:
  port: 8083

spring:
  datasource:
    url: jdbc:h2:mem:loandb
    driver-class-name: org.h2.Driver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info